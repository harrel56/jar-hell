export const PackagePage = () => {
  // const data = useLoaderData() as PackageLoaderData

  return (
    <div>
      {/*<ul>*/}
      {/*  {data.versions.map(version => (*/}
      {/*    <li>{version}</li>*/}
      {/*  ))}*/}
      {/*</ul>*/}
    </div>)
}